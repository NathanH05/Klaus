


//var sel = document.getElementById('typeSelect');
var loop;
var saleLabel =document.getElementsByTagName("sally");

    var sels = document.getElementsByTagName("typeSelect");
    for(var i = 0, maxI = sels.length; i < maxI; ++i) {
      var sel = sels[i];
      if(sel == slctFild)
        continue;  // exclude the current select element
      sel.selectedIndex = 0;
    };
var loop =0;
function preparePage() {
	// used throughout the examples below

// used throughout the examples below

                                      
	loop++;
loop.onchange = function(){

setTimeout(preparePage, 1000);
}
if (document.getElementById("typeSelect").selectedIndex == "0"){
sally.setAttribute("display","none");
change = "select Index must be 0";
                                    
	loop++;
loop.onchange = function(){

setTimeout(preparePage, 1000);
}
}
else{
  sally.setAttribute=("display","inline-block");
 change ="select Index must be 1=<";
                                  
	loop++;
loop.onchange = function(){
console.log(loop);
setTimeout(preparePage, 1000);
}  


};


};window.onload = 
function(){

setTimeout(preparePage, 1000);

};